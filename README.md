docker compose -f docker-compose.yml -f docker-compose.backend-dev.yml exec backend python -c "from app.database import SessionLocal; from app.models import Submission, EvaluationMetadata, SubmissionStatus; from sqlalchemy import func; db=SessionLocal(); ids=[r[0] for r in db.query(Submission.id, func.count(EvaluationMetadata.id)).outerjoin(EvaluationMetadata, EvaluationMetadata.submission_id==Submission.id).filter(Submission.status.in_([SubmissionStatus.done, SubmissionStatus.failed, SubmissionStatus.cancelled])).group_by(Submission.id).having(func.count(EvaluationMetadata.id)==0).all()]; rows=db.query(Submission).filter(Submission.id.in_(ids)).all(); [setattr(s,'status',SubmissionStatus.pending) or setattr(s,'error_message',None) or setattr(s,'acc_score',None) or setattr(s,'eff_score',None) for s in rows]; db.commit(); print('reset to pending', ids); db.close()"